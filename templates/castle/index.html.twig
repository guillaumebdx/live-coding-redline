{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Create Castle{% endblock %}

{% block body %}
    <div class="container">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Créer ce château</button>
    {{ form_end(form) }}
    </div>

{% endblock %}
